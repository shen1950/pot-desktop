## 1.4.0 (2026-08-23)

### New feature:

-   追问模型候选纳入识别/翻译 AI 插件实例：listUsableChatInstances 统一收集内置 OpenAI 兼容实例与带 apiKey/requestPath 的插件实例（标签带 [插件] 后缀），URL 拼接兼容 /v1 结尾端点避免 /v1/v1 重复

### Bugs fixed:

-   package.json 脚本改为直调 node vite，规避本机 pnpm 包装器随机崩溃（libuv 断言）

## 1.3.0 (2026-08-22)

### New feature:

-   OCR 支持带图追问（视觉模型多模态输入）：识别窗口新增「带图追问」按钮，Chat 窗口发送时以 OpenAI 视觉格式注入原图，输入框上方显示缩略图附件指示条，可一键移除

## 1.2.0 (2026-08-22)

### New feature:

-   追问对话窗口标题栏新增模型切换下拉框：候选为所有可用 OpenAI 兼容实例，切换立即对当前会话生效并持久化为默认追问服务，与设置页下拉框联动

## 1.1.0 (2026-08-22)

### New feature:

-   新增 AI 追问服务设置，追问与翻译模型解耦：翻译设置页新增专用下拉框，抽取 resolveChatLlmConfig 统一解析优先级，翻译窗口当前服务非 LLM 时也可追问

## 1.0.0 (2026-08-22)

Fork from pot, application identifier changed to pot-guling

### New feature:

-   OCR 追问按钮扩展到所有识别服务（无 AI 插件时回退使用翻译服务中第一个 OpenAI 兼容 LLM 实例）
-   移植朋友的 AI 追问对话窗口（翻译后多轮问答），流式输出、消息编辑、重新生成、可折叠系统提示词
-   为划词/输入/截图翻译增加双快捷键入口（显示窗口 + 新建窗口）
-   支持多个置顶翻译窗口
-   恢复备份时使用当前应用的 bundle identifier 目录，确保 pot-guling 恢复后配置生效

### Bugs fixed:

-   备份恢复后各窗口自动刷新配置（语言/主题/字体），不再显示旧值
-   字体/回退字体选择器改为 Autocomplete 支持自由文本输入，避免输入时组件反复卸载重挂
-   展开翻译条目后才显示翻译错误，不再被折叠状态吞掉
-   single-instance 插件改为仅 release 模式生效，规避开发环境空指针

## 3.0.7 (2025-5-10)

### New feature:

-   signed macOS app

### Bugs fixed:

-   fix screenshot on macOS
-   rm tray click event on macOS
